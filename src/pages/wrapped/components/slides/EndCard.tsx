import { Typography } from '@mui/material';

import WrappedSchaffold from '../WrappedSchaffold';

const EndCard = () => {
  return (
    <WrappedSchaffold spacing='large' variant='vertical'>
      <Typography variant='h2'>Lyst til å lage kule ting for linjeforeningen din?</Typography>
      <Typography variant='h2'>Søk deg inn i INDEX :-)</Typography>
    </WrappedSchaffold>
  );
};

export default EndCard;
