// const Modal = ({ openPicture, setOpenPicture }) => {
//   const handleClick = (e) => {
//     if (e.target.classList.contains('backdrop')) {
//       setSelectedImg(null);
//     }
//   };

//   return (
//     <div className='backdrop' onClick={handleClick}>
//       <img alt='' src={selectedImg} />
//     </div>
//   );
// };

// export default Modal;
