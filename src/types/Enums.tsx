export enum ThemeType {
  KEY = 'theme-cookie',
  DARK = 'dark',
  LIGHT = 'light',
  AUTOMATIC = 'automatic',
}

export enum WarningType {
  ERROR = 0,
  WARNING = 1,
  MESSAGE = 2,
}

export enum RequestMethodType {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  DELETE = 'DELETE',
}

export enum Study {
  DATAING = 'Dataing',
  DIGFOR = 'DigFor',
  DIGSEC = 'DigSec',
  DIGSAM = 'DigSam',
}

export enum CheatsheetType {
  FILE = 'FILE',
  GITHUB = 'GITHUB',
  LINK = 'LINK',
  OTHER = 'OTHER',
}
